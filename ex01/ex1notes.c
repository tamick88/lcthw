// pulls in a library
#include <stdio.h>

/* This is a comment. */
int main(int argc, char *argv[])
{
	// declares a variable, stores value in it
	int distance = 100;
	
	// this is also a comment
	printf("You are %d miles away.\n", distance);
	
	// returns an exit value of zero to the os
	return 0;
}
